mod token;
mod lexing;
mod parsing;
mod calculus;

fn main() {
    /*let lexed = lexing::lexer("16");
    let parsed = parsing::parser(lexed);

    let result = calculus::calculator(parsed);
    println!("{result}");*/
}